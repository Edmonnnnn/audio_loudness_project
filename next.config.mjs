/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/lufs',
  // если нужны абсолютные пути к ассетам за CDN  добавим assetPrefix
  // assetPrefix: '',
  reactStrictMode: true,
};
export default nextConfig;
